# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage

# Node / frontend
node_modules/
dist/
.vite/
*.tsbuildinfo

# Editors / OS
.vscode/
.idea/
.DS_Store

# Env
.env
.env.local

# Local persistence (Stage 2)
backend/aegis.db
backend/data/

# ML training corpus / artifacts (M3) — never commit raw datasets; ml/models/ is ignored except
# for the committed documentation (CARD.md, metrics.json) carved out below. The actual binary
# model artifacts live in backend/app/ml/artifacts/ instead (see that directory) — not ignored,
# since Docker's build context can only see files inside backend/.
ml/data/
ml/models/*

# Threat-intelligence enrichment (backend/scripts/refresh_threat_intel.py) — raw downloaded
# feed data, never committed. The normalized, deduped snapshots this script PRODUCES live in
# backend/app/threat_intel/data/ instead (committed, small) — same split as ml/data/ above.
data/raw/threat_intel/
!ml/models/CARD.md
!ml/models/metrics.json
